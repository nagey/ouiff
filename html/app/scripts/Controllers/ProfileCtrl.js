/*global define */
define(['angular', 'jquery'], function (angular, $) {
    'use strict';

    return function ProfileCtrl($scope, $routeParams, user) {
        console.log('ProfileCtrl',$routeParams);
    };
});